import { SchemaProperty } from './components/form-element';
export interface Conditional {
    compiled: string;
    then?: SchemaProperty;
    else?: SchemaProperty;
}
export declare const isObject: (item: any) => boolean;
export declare const addProperties: (currentObject: any, newProperties: any) => any;
export declare const getConditionals: (schema: SchemaProperty) => Conditional[];
