import { ReactNode } from 'react';
import { SchemaProperty } from './form-element';
interface ButtonElementProperties {
    property: SchemaProperty;
    value: string;
    onChange: () => void;
    children: ReactNode;
}
export default function ButtonElement({ property, value, onChange, children }: ButtonElementProperties): JSX.Element;
export {};
