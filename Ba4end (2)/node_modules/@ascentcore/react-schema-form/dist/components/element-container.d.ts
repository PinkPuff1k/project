import { ReactNode } from 'react';
interface ElementContainerProperties {
    children: ReactNode;
}
export default function ElementContainer({ children }: ElementContainerProperties): JSX.Element;
export {};
