import { ReactNode } from 'react';
import { SchemaProperty } from './components/form-element';
export interface RegistryKeys {
    [key: string]: {
        component?: ReactNode | string;
        wrapper?: ReactNode;
    };
}
export default class ComponentRegistry {
    _registry: RegistryKeys;
    _wrapper: ReactNode;
    _exceptions: {
        paths?: RegistryKeys;
        keys?: RegistryKeys;
    };
    constructor(customRegistry?: any, wrapper?: ReactNode, exceptions?: {
        paths?: RegistryKeys;
        keys?: RegistryKeys;
    });
    getComponent(property: SchemaProperty, itemValue: any, handleChange: (value: string | number | boolean) => void, children?: ReactNode | null): JSX.Element;
}
