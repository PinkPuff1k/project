import { ReactWrapper } from 'enzyme';
export interface QueryOutput {
    labelText: string | number;
    inputValue: string | string[] | null;
    errorText: string | number;
    input: ReactWrapper | null;
}
export declare function getComponentTree(schemaComponent: ReactWrapper, wrapperSelector?: string, labelSelector?: string, errorSelector?: string, componentSelectors?: string[]): QueryOutput[];
export declare function populateTree(tree: any[], list: any[]): void;
export declare function getByCSSSelector(component: ReactWrapper, selector: string): ReactWrapper;
